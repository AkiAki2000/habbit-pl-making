# 習慣PL

複数の習慣科目（例: 睡眠、トレーニング・運動、深酒、たばこ）を、日々の達成度を損益（円）に換算して記録するシンプルなWebアプリです。

## できること

- 習慣科目を好きなだけ登録・編集（削除も可）
- 科目ごとに「達成」「該当なし」「未達」の換算金額を個別に設定
- 科目ごとにセグメント（健康/家族/自己投資/デジタル習慣）を設定
- その日ある科目をまとめて記録（各科目3択ボタン）し、「本日を確定する」で
  その日の入力をロック（あとから「編集し直す」でいつでも解除可能）
- 未来の日は先取りして記録できない一方、記録し忘れた日があれば、古い日
  から順に1日ずつ確定していく形でさかのぼって記録できる（該当日が0件でも
  確定して次の日に進める）
- 週の始まり（月曜）から今日までの週次損益と、累計損益を画面上部に表示
- 期間（直近7日/直近30日/今四半期/年初来）を切り替えて、セグメント別・科目別の
  損益と達成率を確認できる期間レポート（達成率は「該当なし」を除いて計算）
- 記録した日付ごとの履歴を一覧表示（科目・評価・金額）

データはブラウザのローカルストレージにのみ保存されます（サーバー・DBなし）。
以前の単一科目版のデータがある場合は、初回起動時に自動で移行されます。

## 開発

```bash
npm install
npm run dev
```

[http://localhost:3000](http://localhost:3000) を開いてください。

## 技術構成

- Next.js (App Router) + TypeScript
- Tailwind CSS
- 状態保存: localStorage
